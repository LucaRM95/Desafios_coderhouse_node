<h1>Carrito</h1>

<table id="main" role="grid">
    <tr>
        <th>Titulo</th>
        <th>Descripción</th>
        <th>Thumbnail</th>
        <th>Precio</th>
        <th>Cantidad</th>
    </tr>
    {{#each payload}}
    <tr>
        <td>{{this.pid.title}}</td>
        <td>{{this.pid.description}}</td>
        <td>{{this.pid.thumbnail}}</td>
        <td>{{this.pid.price}}</td>
        <td>{{this.quantity}}</td>
    </tr>
    {{/each}}
</table>

<a href="http://localhost:8080/api/products?limit=2&page=1" role="button">Productos</a>
<a href="http://localhost:8080/auth/logout" role="button">logout</a>